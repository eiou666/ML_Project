# Fashion-MNIST 半监督学习课设实验记录

最后更新：2026-09-09；全部正式训练与汇总于 **2026-09-09 23:02:22（北京时间）完成**。

## 记录约定与当前结论

本文件记录从环境配置、队列执行到全部 77 组正式实验完成的完整技术档案。数值以本机保存的配置、检查点、预测文件、训练日志和统计表为依据。未运行的实验不填写推测结果。

本机项目目录为 `C:\Codex_Project\ML_Project`。文件路径均相对于该目录。

| 阶段 | 完成情况 | 说明 |
|---|---:|---|
| 环境与算法链路验证 | 已完成 | 完成 CUDA 环境验证、合成数据与真实数据单测及链路打通 |
| 正式主实验 | 50/50 | 10 个方法/标签条件，每个 5 个 seed |
| 阈值消融 | 9/9 条件 | 新增 6 次训练，另外 3 次复用主实验 |
| 主动学习 | 21/21 次训练 | 3 个 seed，每个 1 个共享初始模型及 3 策略 × 2 轮新模型 |
| 实际独立训练总数 | **77/77** | 每次均完成 12,000 步；不重复计算复用模型 |
| 自动聚合 | 已完成 | 77 条运行记录、27 条主动学习历史、统计表及 6 张 PNG 图 |
| 课程设计最终报告正文 | 待撰写 | 本文件是实验记录，自动汇总文件不是最终提交报告 |

完成证据：[队列最终状态](outputs/local-training/status.json) 为 `state=complete, phase=done`，完成阶段为 `main / ablation / active_learning / aggregate`；[聚合日志](outputs/local-training/aggregate.log) 记录 `runs=77`。本次核验逐一核对了 77 个配置、划分、检查点、预测文件和最终指标。

## 一、研究问题与正式实验矩阵

| 研究问题 | 实验安排 | 解释目标 |
|---|---|---|
| RQ1：减少标签会损失多少性能？ | Supervised：100%、10%、5%、1%，seed 0–4 | 建立全标签参考及各少标签监督基线 |
| RQ2：半监督能否弥补标签不足？ | Pseudo-label、FixMatch：10%、5%、1%，seed 0–4 | 与同预算、同 seed 的监督结果配对比较 |
| RQ3：伪标签质量与置信度阈值有什么关系？ | FixMatch、1% 标签，τ=0、0.8、0.95，seed 0–2 | 比较覆盖率、伪标签正确率和最终测试表现 |
| RQ4：联合不确定性和多样性能否节省标签？ | Random、Entropy、Uncertainty + diversity；1%→2%→3%，seed 0–2 | 比较主动学习曲线和曲线下面积 |

计数：监督 `4×5=20`，两种半监督 `2×3×5=30`，主实验共 50 次；τ=0.95 的三个消融模型直接复用主实验 seed 0–2，因此消融只新增 `2×3=6` 次；主动学习 `3×(1+3×2)=21` 次。合计 `50+6+21=77`。

所有正式条件均使用同一骨干、优化器、学习率方案和 12,000 次参数更新。相同 seed、相同标签预算的监督/半监督使用相同划分。这里控制的是更新次数，不代表各方法计算量或实际耗时相同。

## 二、本机环境、安装和数据协议

### 2.1 最终使用的环境

| 项目 | 本轮实测/记录值 |
|---|---|
| 操作系统、终端 | Windows x64、PowerShell |
| CPU | Intel Core i5-12600KF，10 核、16 逻辑处理器 |
| GPU | NVIDIA GeForce RTX 3060 Ti，8 GiB 显存 |
| NVIDIA 驱动 | 560.94（本轮配置时检测） |
| Python | 3.12.10，项目 `.venv\Scripts\python.exe` |
| PyTorch / torchvision | 2.5.1+cu121 / 0.20.1+cu121 |
| CUDA runtime | 12.1；`cuda_available=true` |
| NumPy / SciPy | 1.26.4 / 1.17.1 |
| pandas / scikit-learn | 2.3.3 / 1.9.0 |
| matplotlib / seaborn | 3.11.1 / 0.13.2 |
| pytest | 8.4.2 |

环境依据：[environment.json](outputs/environment-check/environment.json)、[本机完整依赖锁](requirements-local-lock.txt)。`requirements.txt` 是兼容范围，`requirements-cuda.txt` 是本机 CUDA 安装约束；旧 `requirements-lock.txt` 和早期 CPU 环境记录不代表本轮实际环境。

原虚拟环境关联旧电脑解释器，不能直接沿用。已建立 Python 3.12 的新 `.venv`，原环境保留；本次核验其实际目录为 `venv-legacy-20260908-224459/`，不在运行环境中。当前安装脚本以后遇到失效环境会备份到 `tmp/venv-legacy-时间戳/`。

安装入口为 [setup_cuda.ps1](scripts/setup_cuda.ps1)：选择 Python 3.11–3.12、创建或修复虚拟环境、安装指定 CUDA wheel、安装依赖并检查冲突、导出本机版本锁、验证 CUDA 及运行测试。CUDA runtime 随 PyTorch wheel 安装，本轮无需另装完整 CUDA Toolkit。下载较慢时使用 [download_cuda_wheel.py](scripts/download_cuda_wheel.py) 从官方源分段下载并校验长度及 SHA-256；缓存和临时安装文件位于 `tmp/`，不属于实验结果。

选择本机的实际原因是数据已经在本地、环境与输出更容易控制、无需依赖云端会话配额。没有本项目同配置的 T4 对照计时，因此不能把“3060 Ti 比 T4 快几倍”写成实验结论，也不沿用原先未经验证的 T4 每组 5–8 分钟估计。

### 2.2 数据集与标注预算

使用官方 Fashion-MNIST：训练 60,000 张、测试 10,000 张，28×28 单通道灰度图，10 类；标签顺序为 T-shirt/top、Trouser、Pullover、Dress、Coat、Sandal、Shirt、Sneaker、Bag、Ankle boot。数据位于 `data/FashionMNIST/raw/`，本地正式配置 `download=false`。

| 标注比例 | 总标注数 | 用于训练的标签 | 验证标签 | 无标签池 |
|---:|---:|---:|---:|---:|
| 100% | 60,000 | 54,000 | 6,000 | 0 |
| 10% | 6,000 | 5,400 | 600 | 54,000 |
| 5% | 3,000 | 2,700 | 300 | 57,000 |
| 1% | 600 | 540 | 60 | 59,400 |

验证标签计入总预算，不能把“1% 标签”写成 600 张训练标签。各类分层抽样；同 seed 的已标注集合按 1%⊂5%⊂10%⊂100% 嵌套，但不同预算的验证集合并非固定不变。嵌套减少部分抽样差异，不意味着完全消除了随机性或验证集规模的影响。

标准化只使用官方训练集的图像，未使用测试集统计量：`mean=0.2860405743122101`，`std=0.3530242443084717`。无标签样本的真实类别不进入损失或主动选样；隐藏标签仅在无梯度诊断路径统计伪标签正确率，或在选样完成后模拟人工揭示标签。

依据：[data.py](src/data.py)、[splits.py](src/splits.py)，各运行的 `split_indices.npz` 与 `split_summary.json`。

## 三、模型、损失与训练配置

正式基础配置：[configs/local_cuda.yaml](configs/local_cuda.yaml)，本次文件 SHA-256 为 `d490e8427e9042a728d48520cef11936a617a8beda35e4a6463f7cca25d42dc6`。每次运行的覆盖参数保存于对应 `resolved_config.yaml`；`metrics.json` 另存配置哈希、划分哈希和二者组合哈希。

| 参数 | 正式取值 |
|---|---|
| 骨干 | FashionCNN，1,147,722 个可训练参数 |
| 结构 | 通道 64→128→256；每块两层 3×3 Conv + BatchNorm + ReLU；前两块 MaxPool；全局平均池化；256 维特征；Dropout 0.20；10 类线性头 |
| 初始化 | 卷积 Kaiming normal；BatchNorm 权重 1/偏置 0；分类头 normal(std=0.01) |
| 优化器 | AdamW，目标学习率 0.003，weight_decay=0.0001 |
| 调度 | 前 500 步 warmup，随后余弦衰减 |
| 每组训练 | 12,000 步，固定结束；未使用早停或按验证成绩选择最佳检查点 |
| 批量 | 标注 64；半监督无标签 192，即无标签/标注比为 3 |
| 主方法 τ / λu | 0.95 / 1.0；消融覆盖 τ=0、0.8 |
| 梯度裁剪 | 最大范数 5.0 |
| EMA / AMP | EMA decay=0.999；CUDA AMP 开启 |
| 记录与保存 | 每 100 步控制台日志；每 500 步验证并保存检查点，共 24 个历史记录点 |
| 评估 | 最终第 12,000 步的 EMA 模型；验证、测试 batch=256，当前实现评估 worker=0 |
| 确定性 | 固定 seed 和按 step 的随机种子；cuDNN deterministic=true、benchmark=false；确定性算法采用 warn_only 模式 |
| 最终执行方式 | 主进程 PyTorch CPU 线程 4；训练预取进程 4；锁页内存；`CUBLAS_WORKSPACE_CONFIG=:4096:8` |

配置里的 `data.num_workers=2` 不等于确定性训练实际预取进程数。确定性训练由 `ML_PREFETCH_WORKERS` 控制，最终后台默认 4；验证/测试已使用单进程取数，主动学习池推理仍读取配置中的 worker 数。执行期线程数、预取数属于运行设置，不修改研究配置哈希；新结果在 `execution` 字段记录相关值。

三种方法的实现口径：

- **Supervised**：只对标注批次计算交叉熵。
- **Pseudo-label**：当前训练模型在弱增强图像上无梯度预测，最高概率类别为硬伪标签；另一个独立弱增强视图学习该伪标签。
- **FixMatch**：同样用弱增强生成伪标签，但用强增强视图学习。总损失 `L = Ls + λu·mean(1[max(p)≥τ]·CE(logits_target, pseudo_target))`；无监督损失除以整个无标签批量，不是只除以被接纳的样本数。伪标签由当前模型生成，最终评估使用 EMA。

弱增强：反射填充 4 像素、随机裁剪回 28×28、随机水平翻转、ToTensor、Normalize。强增强另外使用 RandAugment（2 个操作、magnitude=9）及 RandomErasing（p=0.25、面积比例 0.02–0.20、随机填充值）。验证和测试只进行 ToTensor、Normalize。

本项目是统一骨干和训练协议下的 FixMatch 风格对照实现，不声称完全复现原论文的网络、优化器及所有训练细节。源码依据：[模型](src/models/fashion_cnn.py)、[损失](src/methods/pseudolabel.py)、[训练循环](src/engine.py)。

## 四、执行时间线、故障与恢复

所有时间均为北京时间。重新启动队列会先核验、复用已完成运行，因此同一阶段多次出现 `Started` 不代表所有模型重新训练。

| 时间 | 事件与证据 |
|---|---|
| 09-06 至 09-07 | 完成环境检查、合成数据试跑、真实数据下载、划分测试以及算法链路验证 |
| 09-08 晚间 | 重建本机 CUDA 环境，17 项测试通过，三种方法各完成真实数据 3 步 GPU 验证 |
| 09-08 23:00:01 | 首次启动本地正式队列，见 main.log |
| 09-08 23:41:59 | 日志记录一次恢复启动；复用完成模型并续训未完成模型 |
| 09-09 12:55:04 | 再次恢复主实验；当时已完成 43 组，第 44 组尚未结束 |
| 09-09 13:10 起 | 对核实身份的队列应用 4 worker 预取；第 44 组从 4,000 步检查点继续，日志中 4,100→4,200 步约 4 秒 |
| 09-09 14:13:17 | 首次进入消融阶段，表明 50 组主实验已经完成 |
| 09-09 下午 | 消融出现模块导入及内存错误，恢复后继续；15:51:26 首次进入主动学习 |
| 09-09 15:51 / 16:00 两次主动学习启动后 | 先后出现 4 worker、2 worker 的共享文件映射错误 1455；仅减少 worker 没有彻底解决 |
| 09-09 19:38:23 | 主动学习改用 0 worker 串行执行，速度明显下降 |
| 09-09 21:19 核验 | 队列实际已经退出但状态文件残留 running；完成 67/77，未完成项 seed1/entropy/round2 的检查点为 3,500 步 |
| 09-09 21:25:16 | 修复批次传输后恢复后台队列；21:28:08 重新进入主动学习，从 3,501 步继续 |
| 09-09 22:38 | 完成 74/77；seed2/entropy/round2 运行到约 3,200 步，进程与日志均正常 |
| 09-09 23:02:17–23:02:22 | 全部 77 组完成，自动聚合成功，队列写入 complete |

从首次正式启动到最后汇总完成的日历跨度约 **24 小时 2 分 21 秒**，其中包括中断、诊断、修改、重启、复用检查及汇总，不能称为纯 GPU 训练时间。

### 4.1 观察到的异常及已采取的措施

- **旧环境失效、安装下载慢**：使用新 Python 3.12 虚拟环境、明确的 CUDA wheel 和本机依赖锁；下载采用分段与校验，临时目录放在项目 `tmp/`。
- **队列停止但状态仍为 running**：状态文件仅在阶段切换等时刻写入，父进程被终止时可能来不及更新。必须同时看 PID 是否存在、日志是否更新和 GPU 状态。恢复使用原配置及检查点，不使用 `--force` 清空结果。
- **早期停止的原因证据有限**：此前现场诊断记录中，训练停止附近同时出现 Codex 连接关闭和 NVIDIA `nvlddmkm` 事件 153，未能确定唯一原因。这是历史旁证，不能把后续所有停止都归因于驱动、软件关闭或睡眠。
- **消融异常**：`ablation.log` 留有 `ModuleNotFoundError: sympy.polys.domains.ring`、`RuntimeError: bad allocation`、`MemoryError` 和 worker 退出记录。后续恢复完成了全部消融；仅从异常日志无法确认每次错误的唯一根因。当前验证/测试取数已设为 worker=0，减小多进程启动开销和压力。
- **主动学习共享内存错误**：`active_learning.log` 明确记录 `Couldn't open shared file mapping ... error code: <1455>`。先降低到 2 worker 仍再次报错，改为 0 worker 后虽可推进但明显变慢；最终修改了预取数据传输路径，恢复 4 worker，最后一轮恢复持续完成剩余全部任务。

检查点包含模型、EMA、优化器、学习率调度器、AMP scaler、步数、配置组合哈希以及 Python/NumPy/PyTorch/CUDA 随机状态，采用临时文件加原子替换保存。每 500 步保存，异常退出可能重算最多约 499 个未保存步骤。另修复了恢复 RNG 状态时需转回 CPU 张量的问题，以及完成结果复用时的配置/划分哈希检查。主动学习的完整历史快速跳过逻辑不能代替配置审计；改变配置时应使用独立输出目录。

队列以文件锁防重复启动，并通过 `SetThreadExecutionState` 临时阻止自动睡眠；它不能阻止手动睡眠、关机、重启。隐藏窗口后台启动不应被描述为已保证完全独立于 Codex 生命周期，本轮没有改为 Windows 任务计划程序。训练数据已经本地化，训练本身不依赖持续联网，但进程和电源必须保持正常。

## 五、速度优化的依据、实现和实际效果

### 5.1 为什么 CPU/GPU 没跑满仍然很慢

原确定性取批路径在主进程逐张做 CPU 图像增强，绕过普通训练 DataLoader 的并行预取。监督每步处理 64 个标注视图；半监督还要处理 192 个无标签样本的两种视图，即合计 448 个图像视图及额外前向计算。CPU 单条准备流程成为瓶颈，GPU 等待供数；总 CPU 利用率低并不代表这条串行路径没有饱和。

不能用监督学习每组约 5 分钟推算整套实验。未优化的普通伪标签每组约 25 分钟，FixMatch 常约 41 分钟，所以只看完成组数容易误判为持续中断。旧的“一组 3–5 分钟”估计不适用于全部方法。

### 5.2 最终实现

1. [src/prefetch.py](src/prefetch.py) 用 4 个独立 CPU 进程提前准备整步的标注、无标签双视图，GPU 同时训练当前批次；有界、有序队列避免无限堆积。
2. 同一步继续使用相同的 seed、抽样顺序和增强顺序，并传回增强后的 Python/NumPy/PyTorch CPU 随机状态；worker 不初始化 CUDA。保持原 CUDA 随机种子逻辑。
3. 最初直接传输 PyTorch 张量触发了 Windows 共享映射分配错误。最终 worker 使用本地普通张量拼批，转换为 NumPy 数组通过队列传输；主进程恢复张量并锁页，避免逐步创建命名共享张量映射。
4. 模型、标签划分、batch、增强分布、学习率、EMA、AMP 和总步数保持不变；没有通过减少实验、关掉确定性或缩小训练量提速。

### 5.3 测量结果与口径

| 测量 | 串行 | 2 worker | 4 worker | 说明 |
|---|---:|---:|---:|---|
| 首次统一 85 步基准，排除前 5 步 | 167.69 ms/步 | 77.72 ms/步 | 40.91 ms/步 | 当时有正式队列资源竞争；约 4.1 倍短基准加速 |
| 晚间修复后的基准 | 196.37 ms/步（105 步） | 未重测 | 39.94 ms/步（305 步） | 均排除前 5 步；约 4.9 倍，步数和当时负载不完全相同 |
| 晚间正式日志 | 100 步约 20–23 秒 | — | 100 步约 4–5 秒 | 实际训练同样提速约 4–5 倍，另有验证、存盘和选样开销 |

晚间 4 worker 基准中，等数据约 17.35 ms/步、计算/传输及主机开销约 22.60 ms/步。第 3,600 与 3,700 步恢复前后的日志 loss 分别仍为 0.2766 和 0.1329；第 4,000 步新检查点成功保存。短时观察的系统提交内存约 20.4 GiB、提交上限约 51.7 GiB；这只是当时内存快照，后续完成全部队列才构成本轮更长时间的执行证据。

以下取 `metrics.json` 的 `runtime_seconds_this_process`，**只统计 `resumed=false` 的完整训练调用**。训练计时包含循环中的验证/检查点及部分初始化，但不包含整个命令启动、数据准备、主动选样和最终测试，不能直接当作端到端时间；组间数据/预算和系统负载也不同。

| 运行类型 | 预取 worker | 完整调用数 | 平均分钟/组 | 范围（分钟） |
|---|---|---|---|---|
| 主实验/消融 Supervised | 0 | 19 | 5.10 | 4.73–5.83 |
| 主实验/消融 Pseudo-label | 0 | 15 | 25.28 | 25.10–25.42 |
| 主实验/消融 FixMatch | 0 | 8 | 41.26 | 40.77–41.61 |
| 主实验/消融 FixMatch | 4 | 11 | 9.46 | 8.49–10.30 |
| 主动学习 FixMatch | 0 | 1 | 49.77 | 49.77–49.77 |
| 主动学习 FixMatch | 2 | 8 | 20.81 | 18.30–22.37 |
| 主动学习 FixMatch | 4 | 9 | 9.48 | 8.36–10.99 |

特别注意：本轮有 6 个 `resumed=true` 结果，其计时仅覆盖最后一次调用。消融 `fixmatch-001pct-seed1-tau0-a26e9195` 曾在训练满 12,000 步后、最终评估完成前中断，恢复时无需继续参数更新，记录的本次训练时间接近 0；这不代表它只训练了瞬间。不要直接把 77 个该字段相加当作完整总耗时。

补充证据：[性能说明](docs/performance.md)、`outputs/performance/*.json`、三阶段正式日志。基准脚本重复运行会覆盖同名 JSON，早期数值在本记录和性能说明中保留。

## 六、正式结果与统计解释

### 6.1 统计口径

- 以下准确率以百分数表示，`±` 默认是跨 seed 的**样本标准差**，不是 95% 置信区间；Macro-F1 为 0–1。
- 主实验每条件 n=5（seed 0–4）；阈值消融和主动学习 n=3（seed 0–2）。不挑最好的一次作为最终成绩。
- `main_summary.csv` 另含基于 Student t 的 95% 均值置信区间半宽；配对增益先计算同 seed 的 FixMatch−Supervised，再对 5 个差值计算均值和 95% 区间。百分点与相对百分比区分表述。
- `label_efficiency_curve.png` 和主动学习曲线的误差线是标准差，`paired_ssl_gain.png` 是配对均值增益的 95% 区间；不要混用。
- 伪标签覆盖率/正确率汇总来自每个模型 `history.csv` 的最后一个统计窗口（本轮最后 500 步），不是整个 12,000 步的平均值。

### 6.2 RQ1/RQ2：主实验结果

| 方法 | 总标签预算 | n | 测试准确率 %（均值 ± SD） | Macro-F1 均值 |
|---|---|---|---|---|
| Supervised | 100% | 5 | 93.718 ± 0.116 | 0.9370 |
| Supervised | 10% | 5 | 90.352 ± 0.194 | 0.9033 |
| Pseudo-label | 10% | 5 | 91.100 ± 0.126 | 0.9105 |
| FixMatch | 10% | 5 | 91.644 ± 0.134 | 0.9163 |
| Supervised | 5% | 5 | 88.676 ± 0.181 | 0.8863 |
| Pseudo-label | 5% | 5 | 89.596 ± 0.227 | 0.8949 |
| FixMatch | 5% | 5 | 90.454 ± 0.186 | 0.9044 |
| Supervised | 1% | 5 | 82.462 ± 1.130 | 0.8231 |
| Pseudo-label | 1% | 5 | 83.262 ± 0.444 | 0.8272 |
| FixMatch | 1% | 5 | 85.396 ± 1.767 | 0.8534 |

来源：[main_summary.csv](outputs/summary/main_summary.csv)、[main_results.md](outputs/summary/main_results.md)。上表仅列主实验 τ=0.95 的 10 个条件；汇总文件还包含消融的其他阈值。

全标签监督平均准确率 93.718%；只用 10% 总标签的 FixMatch 为 91.644%，低 2.074 个百分点，达到全标签基线准确率的 97.787%。随着标签减少，监督性能下降；本组实验中 Pseudo-label 与 FixMatch 均提高了少标签预算下的均值，FixMatch 均值更高。

| 标签预算 | FixMatch−Supervised 平均增益（百分点） | 配对均值 95% 区间（百分点） | 配对数 |
|---:|---:|---:|---:|
| 1% | +2.934 | [1.696, 4.172] | 5 |
| 5% | +1.778 | [1.680, 1.876] | 5 |
| 10% | +1.292 | [1.155, 1.429] | 5 |

来源：[paired_ssl_gain.csv](outputs/summary/paired_ssl_gain.csv)。三个区间均在 0 以上，支持本协议下存在正向增益；仍是有限 seed 的结果，不能据此保证任意数据集或任意训练配置都有相同效果。1% 标签下 FixMatch 标准差较大（约 1.767 个百分点），稳定性也应报告。

### 6.3 RQ3：阈值消融，严格使用共同 seed 0、1、2

| τ | n | 测试准确率 %（均值 ± SD） | Macro-F1 均值 | 覆盖率 % | 已接纳伪标签正确率 % |
|---|---|---|---|---|---|
| 0 | 3 | 84.883 ± 0.497 | 0.8475 | 100.000 | 84.320 |
| 0.8 | 3 | 85.130 ± 0.803 | 0.8495 | 91.665 | 88.231 |
| 0.95 | 3 | 85.560 ± 0.582 | 0.8540 | 85.286 | 91.185 |

覆盖率从 τ=0 的 100% 降至 τ=0.95 的约 85.286%，被接纳伪标签正确率从约 84.320% 升至 91.185%，符合阈值在数量和质量之间做筛选的机制。测试均值随阈值提高而上升，但差异较小且只有 3 个 seed，本轮不宣称 τ=0.95 被证明为普遍最优。

这里 τ=0.95 的测试均值是 **85.560%（n=3）**，不是主实验的 85.396%（n=5）；比较阈值时不能混用样本数。相同 seed 0–2 的 1% 监督均值为 82.793%，τ=0 的 84.883% 仍高于这一基线，因此本轮没有观察到“无标签数据使平均性能低于监督基线”的结论，不能为迎合 RQ3 预期写成确认偏差已导致性能崩溃。

来源：`outputs/runs/fixmatch-001pct-*/metrics.json` 中共同 seed 的重新统计；[threshold_ablation.png](outputs/summary/threshold_ablation.png) 也按共同 seed 绘制。

### 6.4 RQ4：主动学习协议与结果

每个 seed 先训练 1% 标签的共享初始 FixMatch 模型。三个策略从相同模型、相同池开始；每轮选 600 张并揭示标签，加入训练集合，然后从相同 seed **重新初始化并训练 12,000 步**，不是沿用上一轮权重微调。验证集始终固定为初始 60 张。

| 轮次 | 总标注比例/数量 | 训练标签 | 固定验证 | 无标签池 |
|---:|---:|---:|---:|---:|
| 0 | 1% / 600 | 540 | 60 | 59,400 |
| 1 | 2% / 1,200 | 1,140 | 60 | 58,800 |
| 2 | 3% / 1,800 | 1,740 | 60 | 58,200 |

Random：池中不放回随机抽取。Entropy：预测熵最高的 600 张。Uncertainty + diversity：先取熵最高的 `5×600=3,000` 个候选，再对归一化的 256 维特征用 k-means++ 选取 600 个代表点；选样随机种子为 `seed×10000+round×101`。后续选样不是按真实类别分层，真实标签仅在选定之后写入 `round*_selection.csv`。

| 策略（n=3） | 1% 准确率 % ± SD | 2% 准确率 % ± SD | 3% 准确率 % ± SD | 3% Macro-F1 | AULC 均值 ± SD |
|---|---|---|---|---|---|
| Random | 85.560 ± 0.582 | 88.113 ± 0.648 | 89.600 ± 0.443 | 0.8958 | 0.017569 ± 0.000107 |
| Entropy | 85.560 ± 0.582 | 88.177 ± 0.335 | 89.743 ± 0.265 | 0.8982 | 0.017583 ± 0.000052 |
| Uncertainty + diversity | 85.560 ± 0.582 | 89.507 ± 0.058 | 90.487 ± 0.102 | 0.9054 | 0.017753 ± 0.000040 |

AULC 先按每个 seed 在 x=0.01、0.02、0.03 上做梯形积分，再跨 seed 汇总；x 是比例而非 1、2、3，面积**没有除以区间宽度 0.02 归一化**。表中 AULC 的 `±` 同样为样本标准差。

联合策略在本轮 2%、3% 预算下均值最高：相对随机策略分别高约 1.393 和 0.887 个百分点。3% 标签达到 90.487%，可作为标签效率的观察结果；与普通监督 10% 的数值接近，但二者验证标签分配和 seed 数不同，不能据此声称已经做了严格等价预算检验。Entropy 与 Random 很接近，不能把小幅均值差异夸大为稳定优势。

三个策略在 round0 复用同一模型，所以 `active_runs.csv` 有 `3 seed×3策略×3轮=27` 条历史，实际模型数为 21；初始点的重复引用不是三次独立训练。来源：[active_summary.csv](outputs/summary/active_summary.csv)、[active_runs.csv](outputs/summary/active_runs.csv)、[选样实现](src/active_learning/selection.py)。

### 6.5 错误分析的现有证据

`confusion_1pct.png` 使用 seed0、1% 标签的 Supervised 与 FixMatch，混淆矩阵按真实类别逐行归一化。以下为保存的原始计数（每类测试样本 1,000 张）：

| 真实类别 → 预测类别 | Supervised 错分数 | FixMatch 错分数 |
|---|---:|---:|
| Shirt → T-shirt | 176 | 159 |
| Shirt → Coat | 159 | 108 |
| Shirt → Pullover | 126 | 98 |
| Coat → Pullover | 137 | 85 |

说明衬衫、T 恤、外套和套头衫之间仍有较多混淆，FixMatch 在这些方向有所减少。此处仅是一个指定 seed 的案例，不代表跨 seed 误差均值。`representative_examples.png` 展示同一 FixMatch 模型的高置信正确、高置信错误和最高不确定性样本，每类 4 张；按既定排序选取，不是随机或全面的误差样本。

## 七、验证记录与本次完成性审计

| 时点 | 验证 | 结果 |
|---|---|---|
| 09-07 | 原有划分、模型、损失、主动选样、确定性续训测试 | 15 项通过（历史记录 6.66 秒） |
| 09-08 | 增加 CUDA AMP 恢复、完成结果配置不匹配检查 | 17 项通过，5.68 秒 |
| 09-08 | 三方法真实 Fashion-MNIST、正式 batch 64/192、各 3 步 GPU 验证 | 全部通过；SSL 测试用 τ=0 使无监督项真正参与 |
| 09-09 首次预取优化 | 批次/RNG 一致性及串行检查点切换预取后的 CUDA 模型和 EMA 一致性 | 19 项通过，14.54 秒 |
| 09-09 晚间传输修复 | 新增禁止 worker 创建共享张量存储、仅传数组的回归测试 | 全部 20 项通过，21.29 秒 |
| 本次实验记录补录 | 对完成产物进行只读审计，没有重新训练 | 下列各项全部通过 |

本次产物审计核对了：

1. **77 个 metrics 与 77 个实际检查点**均为第 12,000 步、CUDA，配置与 `local_cuda.yaml` 加对应方法/比例/seed/τ/策略覆盖一致。
2. 保存的配置哈希、组合哈希及重新构造的数据划分相符；各运行的训练/验证/无标签/已标注索引、数量一致。
3. 每组预测均为 10,000×10，概率有限，测试索引完整；从保存预测重算准确率、Macro-F1 和混淆矩阵，与 metrics 一致。
4. 每组历史恰有 24 个验证点（500、1000、…、12000），本轮没有重复 step 行；检查点还保留模型、EMA、优化器、调度器、scaler 和 RNG 状态。
5. 主实验加新增消融恰好 56 个预定条件；主动学习 21 个模型、27 条无重复历史。18 个选样文件每次恰选 600 个唯一池内样本，揭示标签与原数据一致，累计划分及固定验证集一致。
6. 自动汇总的主实验均值与原始指标重算值一致，`runs.csv` 为 77 行、`active_runs.csv` 为 27 行。

真实数据 GPU 验证的峰值 PyTorch allocated 显存：Supervised 227.95 MiB，Pseudo-label/FixMatch 315.90 MiB；依据 [gpu-training-validation.json](outputs/environment-check/gpu-training-validation.json)。这不是整卡显存占用，不包含桌面、CUDA context 和全部 reserved 内存，也不是所有长训练的峰值保证。

一致性测试支持当前软件/硬件下的检查点和执行路径兼容，不保证跨 PyTorch、CUDA 或硬件版本仍逐位相同。未来更换环境时应重新验证。

## 八、结果文件与复现命令

### 8.1 结果位置与用途

| 位置/文件 | 内容与用途 |
|---|---|
| `outputs/runs/` | 主实验及新增阈值消融，56 个运行目录 |
| `outputs/active/seed*/` | 主动学习 21 个模型、每 seed 的历史及选样 CSV |
| 每组 `resolved_config.yaml` | 实际生效配置 |
| 每组 `split_indices.npz` / `split_summary.json` | 划分原始索引、哈希、数量及类别计数 |
| 每组 `checkpoint_latest.pt` | 第 12,000 步完整训练状态与 EMA，供复核/恢复 |
| 每组 `history.csv` | 训练损失、验证指标、伪标签诊断、进程内 elapsed |
| 每组 `metrics.json` / `test_predictions.npz` | 最终指标、环境/哈希/执行设置及测试概率、标签、索引 |
| [main_results.md](outputs/summary/main_results.md) / [main_summary.csv](outputs/summary/main_summary.csv) | 易读主结果与完整均值、标准差、CI 半宽、性能保留比例 |
| [runs.csv](outputs/summary/runs.csv) / [paired_ssl_gain.csv](outputs/summary/paired_ssl_gain.csv) | 77 条运行索引及同 seed 配对增益 |
| [active_runs.csv](outputs/summary/active_runs.csv) / [active_summary.csv](outputs/summary/active_summary.csv) | 主动学习逐轮记录及曲线/AULC 统计 |
| [标签效率图](outputs/summary/label_efficiency_curve.png) / [配对增益图](outputs/summary/paired_ssl_gain.png) | RQ1/RQ2 |
| [阈值消融图](outputs/summary/threshold_ablation.png) / [主动学习图](outputs/summary/active_learning_curve.png) | RQ3/RQ4 |
| [混淆矩阵](outputs/summary/confusion_1pct.png) / [代表性样本](outputs/summary/representative_examples.png) | 指定 seed 的错误案例分析 |
| `outputs/local-training/` | status、main/ablation/active_learning/aggregate 日志 |

### 8.2 在本机查看、验证和重新生成统计

```powershell
Set-Location -LiteralPath C:\Codex_Project\ML_Project

# 查看最终状态；若将来显示 running，还必须检查对应进程和日志新鲜度
Get-Content outputs/local-training/status.json
Get-Content outputs/local-training/active_learning.log -Tail 15

# 检查环境及代码测试；这些命令不会重新执行正式 77 组训练
.\.venv\Scripts\python.exe scripts/verify_environment.py --require-cuda
.\.venv\Scripts\python.exe -m pytest -q

# 仅从已有结果重新生成统计和图，不重新训练
.\.venv\Scripts\python.exe -m src.aggregate --input outputs --output outputs/summary
```

### 8.3 安装及训练/恢复入口（按需要执行，不是当前仍有待跑任务）

```powershell
Set-Location -LiteralPath C:\Codex_Project\ML_Project

# 新环境安装；本机已完成，不需要为了阅读结果重复安装
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/setup_cuda.ps1

# 后台完整队列：核验并复用已完成结果、恢复检查点、最终聚合
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/start_local_training.ps1
```

需要前台按阶段复现时，明确使用本机正式配置和 seeds：

```powershell
$env:ML_PREFETCH_WORKERS = '4'
$env:ML_TORCH_THREADS = '4'
$env:CUBLAS_WORKSPACE_CONFIG = ':4096:8'
$env:PYTHONUTF8 = '1'

.\.venv\Scripts\python.exe -m src.sweep --suite main --config configs/local_cuda.yaml --device cuda --seeds 0 1 2 3 4
.\.venv\Scripts\python.exe -m src.sweep --suite ablation --config configs/local_cuda.yaml --device cuda --seeds 0 1 2
.\.venv\Scripts\python.exe -m src.sweep --suite active_learning --config configs/local_cuda.yaml --device cuda --seeds 0 1 2
.\.venv\Scripts\python.exe -m src.aggregate --input outputs --output outputs/summary
```

已有完整结果时这些训练命令主要执行复用；不是全新独立重复。若要新实验，复制配置并将 `project.output_dir` 与 `project.active_output_dir` 改为新的隔离目录，保留本轮成果。不应为了查看进度使用 `--force`；它会清理相应已有输出。直接调用 `src.run` 未设置 `ML_PREFETCH_WORKERS` 时默认串行；显式设为 0 可作为回退方式。

### 8.4 后续工作与保存边界

正式矩阵、阈值消融、主动学习与自动统计均已完成，无需继续补跑同一批任务。后续课程报告可引用上述结果和图，并说明样本数、统计口径、失败案例及局限。最终报告正文、人工审阅后的报告排版和远端成果归档不应与“训练完成”混为一谈；本轮完成的是实验及记录补全，没有新增远端发布操作。

保存或迁移结果时至少保留源码、配置、测试、依赖锁、本记录、全部 summary、每组 metrics/history/划分/预测；需要续训或模型复核时还需单独保存 `.pt` 检查点。`.gitignore` 会排除虚拟环境、原始数据、临时文件及多数运行产物，Git 提交本身不等于完整实验备份。原始数据可按既定数据集重新取得，现有检查点不要因清理临时目录而一并删除。

